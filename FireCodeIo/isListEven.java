package FireCodeIo;

/**
 * Created by Ivan on 9/2/2017.
 */
public class isListEven {

    // Using Firecodes ListNode class

    //  Given a singly-linked list, check whether its length is even or odd in a single pass.
    //  An Empty list has 0 nodes which makes the number of nodes in it even

//    public Boolean isListEven(ListNode head) {
//        if (head == null)
//            return true;
//
//
//        int index = 0;
//        ListNode tempHead = new ListNode(head.data);
//
//        while(head.next != null){
//            index++;
//            head = head.next;
//        }
//
//        // check if odd using binary
//        return ((index % 10) & 1) == 1 ? true : false;
//    }
}
